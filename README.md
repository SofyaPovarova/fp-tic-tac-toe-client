# tic-tac-toe-client

GTK-клиент для крестиков-ноликов

## Запуск
Сборка происходит через nix

```
nix-shell
make run       # alternatively, make build && ./tic-tac-toe
```

Для корректной работы папка res должна находиться на одном уровне с исполняемым файлом